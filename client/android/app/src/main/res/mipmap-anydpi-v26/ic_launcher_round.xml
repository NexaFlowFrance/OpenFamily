<?xml version="1.0" encoding="utf-8"?>
<!-- The background must fill the whole 108dp canvas: insetting it like the
     foreground left transparent corners under launcher masks that reveal more
     than the 72dp safe zone. A flat colour also avoids any upscaling. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background" />
    <foreground>
        <inset android:drawable="@mipmap/ic_launcher_foreground" android:inset="16.7%" />
    </foreground>
</adaptive-icon>
